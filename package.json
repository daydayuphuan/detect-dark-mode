{
  "name": "detect-dark-mode",
  "version": "0.0.1",
  "description": "detects if the user is using a black theme in the browser.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "dark mode detect",
    "match media color scheme"
  ],
  "author": "huange",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.0.0",
    "tslib": "^2.0.3",
    "typescript": "^4.1.2"
  }
}
